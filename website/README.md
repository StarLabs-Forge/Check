# CHECK — sitio web

Sitio estático (HTML, CSS y JavaScript). Coloca aquí las páginas, estilos y scripts de la web.

Para previsualizar, abre `index.html` en el navegador o sirve la carpeta con cualquier servidor estático.
